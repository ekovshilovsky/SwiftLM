import XCTest
import TurboQuantKit

final class TurboQuantServingTests: XCTestCase {

    func testChatCompletionsEndpointAcceptsTQModel() {
        // Pending: start the server with a TQ model, send an HTTP
        // request, and verify the OpenAI-format response.
    }

    func testStreamingModeWorks() {
        // Pending: verify SSE streaming with a TQ model.
    }
}
